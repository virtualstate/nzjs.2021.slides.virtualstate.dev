import { h } from "../../../h";
import { Slide } from '../slide';
import { _602_ExampleInformation } from '@virtualstate/examples';

export const AdditionalTypesChildren = (
  <Slide index={2206} id="additional-types-children">
    <h1>Types - Children</h1>
    <pre class="code">{_602_ExampleInformation.cleanerSource.split("\n").slice(0, -1).join("\n").trim()}</pre>
  </Slide>
)
